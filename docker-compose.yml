version: '3.8'

services:
  app:
    build: .
    container_name: travel-quotation-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=your-secret-key-change-this-in-production
    restart: always
